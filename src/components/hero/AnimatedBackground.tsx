'use client'

/**
 * AnimatedBackground - replaced with atmospheric gradient.
 * The floating orbs have been removed in favor of a subtle radial gradient
 * applied directly in HeroSection.
 */
export function AnimatedBackground() {
  return null
}
